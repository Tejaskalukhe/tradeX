import { useSelector } from 'react-redux';
import { User, Mail, Shield, Lock, Wallet, CheckCircle } from 'lucide-react';
import PageHeader from '../../components/common/PageHeader.jsx';
import DemoNotice from '../../components/common/DemoNotice.jsx';

export default function Profile() {
  const { user } = useSelector((state) => state.auth);
  const initials = user?.name?.split(' ').map((part) => part[0]).join('').slice(0, 2).toUpperCase() || 'TU';

  return <div className="tx-fade-in"><PageHeader title="Profile" subtitle="View your authenticated TradeX account." /><DemoNotice>Account details are controlled by the authentication service.</DemoNotice><div className="row g-4"><div className="col-lg-8"><div className="card p-4"><div className="d-flex align-items-center gap-3 mb-4"><div className="d-flex align-items-center justify-content-center rounded-circle" style={{ width: 64, height: 64, backgroundColor: 'var(--tx-primary)', color: '#04201d', fontWeight: 700, fontSize: '1.2rem' }}>{initials}</div><div><h5 className="fw-semibold mb-1" style={{ color: 'var(--tx-text)' }}>{user?.name || 'TradeX User'}</h5><p className="mb-0" style={{ color: 'var(--tx-text-muted)', fontSize: '0.82rem' }}>{user?.email || 'No email available'}</p></div></div><div className="row g-3"><div className="col-md-6"><div className="p-3 rounded-3" style={{ backgroundColor: 'var(--tx-surface-2)' }}><div className="d-flex align-items-center gap-2 mb-2"><User size={16} style={{ color: 'var(--tx-primary)' }} /><span style={{ color: 'var(--tx-text-muted)', fontSize: '0.78rem' }}>Full name</span></div><p className="mb-0 fw-semibold" style={{ color: 'var(--tx-text)' }}>{user?.name || '—'}</p></div></div><div className="col-md-6"><div className="p-3 rounded-3" style={{ backgroundColor: 'var(--tx-surface-2)' }}><div className="d-flex align-items-center gap-2 mb-2"><Mail size={16} style={{ color: 'var(--tx-primary)' }} /><span style={{ color: 'var(--tx-text-muted)', fontSize: '0.78rem' }}>Email address</span></div><p className="mb-0 fw-semibold text-break" style={{ color: 'var(--tx-text)' }}>{user?.email || '—'}</p></div></div><div className="col-md-6"><div className="p-3 rounded-3" style={{ backgroundColor: 'var(--tx-surface-2)' }}><div className="d-flex align-items-center gap-2 mb-2"><Shield size={16} style={{ color: 'var(--tx-primary)' }} /><span style={{ color: 'var(--tx-text-muted)', fontSize: '0.78rem' }}>Role</span></div><p className="mb-0 fw-semibold text-capitalize" style={{ color: 'var(--tx-text)' }}>{user?.role || 'user'}</p></div></div><div className="col-md-6"><div className="p-3 rounded-3" style={{ backgroundColor: 'var(--tx-surface-2)' }}><div className="d-flex align-items-center gap-2 mb-2"><Wallet size={16} style={{ color: 'var(--tx-primary)' }} /><span style={{ color: 'var(--tx-text-muted)', fontSize: '0.78rem' }}>Virtual balance</span></div><p className="mb-0 fw-semibold" style={{ color: 'var(--tx-text)' }}>${Number(user?.virtualBalance || 0).toLocaleString('en-US', { minimumFractionDigits: 2 })}</p></div></div></div></div></div><div className="col-lg-4"><div className="card p-4"><h5 className="fw-semibold mb-3" style={{ color: 'var(--tx-text)' }}>Account status</h5><div className="d-flex align-items-center gap-2 mb-3"><CheckCircle size={18} className="text-success" /><span style={{ color: 'var(--tx-text)' }}>{user?.isActive ? 'Active account' : 'Inactive account'}</span></div><div className="d-flex align-items-center gap-2 mb-3"><Lock size={18} style={{ color: 'var(--tx-text-muted)' }} /><span style={{ color: 'var(--tx-text-muted)' }}>Password protected</span></div><hr style={{ borderColor: 'var(--tx-border)' }} /><p className="mb-0" style={{ color: 'var(--tx-text-muted)', fontSize: '0.8rem', lineHeight: 1.6 }}>Role, account status, and virtual balance are managed securely by the backend and cannot be changed from this screen.</p></div></div></div></div>;
}
